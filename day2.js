// Q.1) Character count (prompt + alert)
// Prompt the user for a word (use prompt()), then alert the number of
// characters of that word Example UI message: "Enter a word:".

// let a = prompt("Enter a word:");
// let count = a.length;
// alert(`Number of characters: ${count}`);


// Q.2)Full name using .concat()
// Prompt the user for firstName and lastName. Use .concat() to form fullName
// (and also show concatenation using +). Alert or console.log the message:
// Hello, <fullName>.


// let a=prompt("entre your first name")
// let b=prompt("entre your last name")
// alert("Hello "+a+b)

// Q.3)Extract with .slice()
// Given the string const s = "JavaScript is fun"; extract the substring "Script" and
// console.log it.

// const s = "JavaScript if fun"
// var n = s.slice(4,10)

// console.log(n)

// Q.4)Split comma list & alert second item
// Prompt for a comma-separated list (example input: apple,banana,cherry). Use
// .split(',') to make an array and alert the second item.

// let list = prompt("Enter a comma-separated list:");
// let arr = list.split(',');

// let secondItem = arr[1].trim();

// alert("Second item: " + secondItem);

// Q.5)Reverse words and show result
// Prompt for a short sentence, split it into words, reverse the words array
// (.reverse()), join the words back with spaces (.join(' ')) and alert the reversed
// sentence.

// let s = prompt("Enter a short sentence:");
// let arr = s.split(" ").reverse().join(" ");
// alert(arr);



